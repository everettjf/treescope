{
  "name": "treescope-viewer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Browser viewer for the Treescope runtime view inspector.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "embed": "node scripts/embed.mjs",
    "release": "npm run build && npm run embed"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^5.4.0",
    "vite-plugin-singlefile": "^2.0.0"
  }
}
