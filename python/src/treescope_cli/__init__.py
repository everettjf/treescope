"""Treescope CLI — a Python client for the Treescope runtime view inspector.

Speaks the same loopback HTTP + WebSocket protocol as the browser viewer and the
Node CLI, so a shell, a script, or a coding agent (LLM) can inspect a running
iOS/macOS/tvOS app's UIKit / AppKit / SwiftUI view hierarchy.
"""

__version__ = "0.2.0"
